#include <stdio.h>

/*
    Return whether or not the file in file name exists
    
    fileName - a null terminated string with the file name writen in ascii characters

    return  true- the file exists
            false - the file does not exists
*/
bool DoFileExists(char *filePath);


int ReadLine(char* filePath, )



